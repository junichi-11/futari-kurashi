import { renderArticlePageV3 } from "./article-template-v2.mjs";
const text=value=>String(value??"").trim();
const esc=value=>text(value).replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[char]));
const xml=esc;
const formatDate=value=>text(value).slice(0,10);
export const renderArticlePage=renderArticlePageV3;

export function renderArticlesIndexPage(origin){return `<!doctype html><html lang="ja"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>JOURNAL｜MARGINの記事一覧</title><meta name="description" content="新婚・同棲・ふたり暮らしの家具を、住まいと過ごし方から選ぶMARGINの記事一覧。"><link rel="canonical" href="${origin}/articles/"><link rel="stylesheet" href="/assets/discovery.css"></head><body><header class="site-head"><nav class="site-nav wrap" aria-label="サイトナビゲーション"><a class="brand" href="/">MARGIN</a><span>JOURNAL</span></nav></header><main><section class="hero"><div class="wrap"><p class="eyebrow">MARGIN Journal</p><h1>暮らしを選ぶための記事。</h1><p>部屋の広さと、ふたりの過ごし方から家具を考える。新しい順に、MARGINの編集記事をまとめています。</p></div></section><section class="section"><div class="wrap"><nav id="filters" class="filter-list" aria-label="記事を絞り込む"></nav><div id="articles" class="article-grid" aria-live="polite"><p class="status">記事を読み込んでいます。</p></div></div></section></main><footer class="site-footer"><div class="wrap"><nav class="footer-links" aria-label="フッターナビゲーション"><a href="/">MARGINトップ</a><a href="/about/">運営者情報</a><a href="/privacy/">プライバシーポリシー</a><a href="/contact/">お問い合わせ</a><a href="/affiliate-disclosure/">アフィリエイト広告について</a><a href="/disclaimer/">免責事項</a></nav></div></footer><script src="/assets/article-cards.js"></script><script>fetch('/articles/index.json').then(r=>{if(!r.ok)throw new Error();return r.json()}).then(data=>{const params=new URLSearchParams(location.search),room=params.get('room'),theme=params.get('theme'),all=(data.articles||[]).filter(a=>a.status==='Published').sort((a,b)=>Date.parse(b.updatedAt||b.publishedAt)-Date.parse(a.updatedAt||a.publishedAt)),shown=all.filter(a=>(!room||a.roomType===room)&&(!theme||(a.editorialThemes||[]).includes(theme)||String(a.mainKeyword||'').includes(theme)));MarginArticleCards.render(document.querySelector('#articles'),shown);const rooms=[...new Set(all.map(a=>a.roomType).filter(Boolean))];document.querySelector('#filters').innerHTML='<a href="/articles/">すべて</a>'+rooms.map(value=>'<a href="/articles/?room='+encodeURIComponent(value)+'">'+value+'</a>').join('');if(!shown.length)document.querySelector('#articles').innerHTML='<p class="status">条件に合う記事はまだありません。</p>'}).catch(()=>document.querySelector('#articles').innerHTML='<p class="status">記事一覧を読み込めませんでした。</p>')</script></body></html>`}

export function renderSitemapXml(index,origin){const fixed=["about","privacy","contact","affiliate-disclosure","disclaimer"].map(path=>({url:`${origin}/${path}/`,lastmod:index.updatedAt})),entries=[{url:`${origin}/`,lastmod:index.updatedAt},{url:`${origin}/articles/`,lastmod:index.updatedAt},...fixed,...(index.articles||[]).map(article=>({url:`${origin}${article.url}`,lastmod:article.updatedAt}))],unique=[...new Map(entries.map(entry=>[entry.url,entry])).values()];return `<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${unique.map(entry=>`  <url><loc>${xml(entry.url)}</loc><lastmod>${xml(formatDate(entry.lastmod))}</lastmod></url>`).join("\n")}\n</urlset>\n`}
export function renderFeedXml(index,origin){const articles=[...(index.articles||[])].sort((a,b)=>Date.parse(b.publishedAt)-Date.parse(a.publishedAt)).slice(0,20),items=articles.map(article=>`<item><title>${xml(article.displayTitle)}</title><link>${xml(`${origin}${article.url}`)}</link><guid isPermaLink="true">${xml(`${origin}${article.url}`)}</guid><pubDate>${new Date(article.publishedAt).toUTCString()}</pubDate><description>${xml(article.metaDescription)}</description></item>`).join("");return `<?xml version="1.0" encoding="UTF-8"?>\n<rss version="2.0"><channel><title>MARGIN Journal</title><link>${xml(`${origin}/articles/`)}</link><description>ふたり暮らしの家具とインテリアを静かに選ぶ</description><language>ja</language><lastBuildDate>${new Date(index.updatedAt).toUTCString()}</lastBuildDate>${items}</channel></rss>\n`}
