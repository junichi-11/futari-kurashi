const json=(body,status)=>new Response(JSON.stringify(body),{status,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store"}});
export async function onRequest({request,env}){if(request.method!=="POST")return json({error:"POSTのみ利用できます。"},405);let origin;try{origin=new URL(env.PUBLISH_VERCEL_ORIGIN||env.RAKUTEN_VERCEL_ORIGIN).origin}catch{return json({error:"PUBLISH_VERCEL_ORIGINを設定してください。"},500)}try{const response=await fetch(new URL("/api/publish/refresh-products",origin),{method:"POST",headers:{"content-type":"application/json","origin":new URL(request.url).origin,"x-margin-publish-key":request.headers.get("x-margin-publish-key")||""},body:await request.text()||"{}"}),text=await response.text();try{return json(text?JSON.parse(text):{},response.status)}catch{return json({error:text||"VercelからJSON以外の応答が返りました。"},502)}}catch(error){return json({error:"Vercelへ接続できません。"},502)}}
